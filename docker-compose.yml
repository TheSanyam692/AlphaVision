version: '3.8'

services:
  alphavision:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/alphavision.db
      - ENVIRONMENT=production
      - PORT=8000
    restart: always
